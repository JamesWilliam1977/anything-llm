interface:
  display_name: "Anything Llm"
  short_description: "Repo-specific patterns and workflows for anything-llm"
  default_prompt: "Use the anything-llm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true